{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://decisiontrees-aicft-assessor/behaviour_opportunities.schema.json",
  "title": "Worksheet Behaviour Opportunities",
  "type": "object",
  "required": [
    "worksheet",
    "behaviour_ontology_reference",
    "note",
    "items"
  ],
  "additionalProperties": true,
  "properties": {
    "worksheet": {
      "$ref": "common.schema.json#/$defs/worksheet_id"
    },
    "curriculum_status": {
      "enum": [
        "deployed",
        "not_deployed"
      ]
    },
    "behaviour_ontology_reference": {
      "type": "string",
      "const": "historical_reference_only_not_a_live_path",
      "description": "Sentinel — not a filesystem path. See note for OB provenance."
    },
    "note": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable provenance; includes archived ECD v1 OB ontology reference."
    },
    "items": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/item"
      }
    }
  },
  "$defs": {
    "opportunity": {
      "type": "object",
      "required": [
        "behaviour_id",
        "role",
        "evidence_mode",
        "rationale"
      ],
      "additionalProperties": false,
      "properties": {
        "behaviour_id": {
          "type": "string",
          "pattern": "^OB_[A-Z]{3}_\\d{3}$"
        },
        "role": {
          "enum": [
            "primary",
            "secondary",
            "supporting"
          ]
        },
        "evidence_mode": {
          "enum": [
            "direct",
            "indirect",
            "diagnostic"
          ]
        },
        "rationale": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "item": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "extraction_fields": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "opportunities": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/opportunity"
          }
        }
      }
    }
  }
}
